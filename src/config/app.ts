import { getBaseUrl } from '@/lib/utils';

export const config = {
	name: 'Charles-AI',
	description:
		'Charles-AI is a platform that allows you to ask questions and get answers based on previously trained legal documents.',
	url: getBaseUrl(),
};
